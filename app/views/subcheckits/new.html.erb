<%= bootstrap_form_for @subcheckit, url: subcheckits_path do |f| %>
  <%= f.content_field :name %>
  <%= f.content_field :category %>
  <%= f.hidden_field :user, value: @user.id %>
  <button class="btn btn-primary">Save</button>
<% end %>
